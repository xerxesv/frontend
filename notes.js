// takes an array of values
// when the user starts typing something in, 
  // if the input is not empty, takes in the current input
  // finds the elements in the array that match that input
  // makes the ul below visible
  // populates the ul with data

// bind events to the input:
  // input
    // when the user starts typing something in, 
      // if the input is not empty, takes in the current input
      // finds the elements in the array that match that input
      // makes the ul below visible
      // populates the ul with data

  // blur
    // when it's unselected hide the list

  // keydown
    // down travels down
    // up travels up
    // enter 'selects' the element (makes it the value of the field)


// bind events to the list
  // mousedown

